{
  "name": "wgpu-engine-web-demo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "wasm-dev": "wasm-pack build ../core --target web --dev --out-dir ../web-demo/pkg",
    "wasm": "wasm-pack build ../core --target web --out-dir ../web-demo/pkg",
    "dev": "npm run wasm-dev && vite",
    "build": "npm run wasm && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.4.0",
    "vite-plugin-top-level-await": "^1.4.0"
  }
}
